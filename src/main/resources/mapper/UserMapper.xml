<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chenyue.blog.dao.UserDao">
    <sql id="tb">user</sql>
    <sql id="baseColumns"></sql>
    <insert id="insert" parameterType="com.chenyue.blog.entity.User" useGeneratedKeys="true" keyProperty="id" >
    </insert>


    <!--<update id="update" parameterType="com.chenyue.entity.User">
        UPDATE <include refid="tb"/>
        <set>
            <if test="username != null and username.length() > 0">
                username = #{username},
            </if>
            <if test="password != null and password.length() > 0">
                password = #{password},
            </if>
            <if test="email != null and email.length() > 0">
                email = #{email},
            </if>
            <if test="birthday != null">
                birthday = #{birthday},
            </if>
            <if test="gender != null">
                gender = #{gender}
            </if>
        </set>
        WHERE id = #{id}
    </update>-->

</mapper>